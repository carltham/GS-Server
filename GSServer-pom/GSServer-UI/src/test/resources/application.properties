# Tests use an isolated, throwaway user store (cleaned by `mvn clean`, seeded from the
# gsserver.security.users defined in the main application.yml).
gsserver.security.users-file=target/test-users.json

# Exercise the localhost thor behaviour in contract tests.
gsserver.security.thor-login-enabled=true
