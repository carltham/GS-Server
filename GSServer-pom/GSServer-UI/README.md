# GSServer-UI Module

Spring Boot module that serves the Angular frontend as static files.

## Structure
- frontend: Angular source workspace
- src/main/resources/static: Browser-delivered build output

## Run (once Maven is installed)
1. cd GSServer-pom/GSServer-UI
2. mvn spring-boot:run

Default URL: http://localhost:8080
