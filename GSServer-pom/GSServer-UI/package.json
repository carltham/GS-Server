{
  "name": "gsserver-e2e",
  "version": "1.0.0",
  "description": "E2E tests for GS-Server using Playwright",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
